{
  "name": "init-me-root",
  "private": true,
  "eslintIgnore": [
    "node_modules",
    "output",
    "test"
  ],
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.2"
  },
  "scripts": {
    "u": "yarn upgrade-interactive --latest",
    "l": "cd packages/init-me && npm link --force",
    "d": "lerna run d",
    "prettier": "lerna run prettier"
  }
}
